# PostgreSQL connection string (leave blank for SQLite)
DATABASE_URL=

# AES encryption key for agent private keys (32-byte base64-encoded)
# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
COGIT_ENCRYPTION_KEY=

# Optional: Polygon RPC URL override
POLYGON_RPC_URL=https://rpc-amoy.polygon.technology
